{
  "name": "hsm",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "pnpm run build && wrangler deploy --env production",
    "deploy:staging": "pnpm run build && wrangler deploy --env staging",
    "deploy:template": "pnpm run build && wrangler deploy",
    "dev": "wrangler dev",
    "build": "node scripts/build.mjs",
    "test": "vitest",
    "test:ci": "vitest --run",
    "cf-typegen": "wrangler types",
    "format": "biome format --write",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/node": "^26.4.1",
    "@vitest/coverage-v8": "^4.1.11",
    "marked": "^18.0.11",
    "meathill-brand": "^0.1.0",
    "typescript": "^7.0.2",
    "vitest": "~4.1.11",
    "wrangler": "^4.129.0"
  },
  "dependencies": {
    "hono": "^4.13.7"
  },
  "cloudflare": {
    "bindings": {
      "KV": {
        "description": "Stores encrypted payloads and envelope metadata."
      },
      "CF_SECRET_PART": {
        "description": "Server-side secret part (Part A) used in HKDF to derive KEK."
      },
      "INDEX_SECRET": {
        "description": "HMAC secret used to obfuscate key paths before KV storage."
      }
    }
  }
}
